<% breadcrumb :samples %>

<h1>Samples</h1>

<table>
  <thead>
    <tr>
      <th><%= Sample.human_attribute_name("name") %></th>
      <th colspan="3"><%= t("views.actions")%></th>
    </tr>
  </thead>

  <tbody>
    <% @samples.each do |sample| %>
      <%= content_tag(:tr, data_to_delete(sample)) do %>
        <tr>
          <td><%= link_to sample.name, edit_sample_path(sample), class: "btn" %></td>
          <td><%= edit_btn edit_sample_path(sample) %></td>
          <td><%= delete_btn sample %></td>
        </tr>
      <% end %>
  <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Sample', new_sample_path, class: "btn" %>

<div class="paging">
  <%= render 'components/page_size' %>
  <%= paginate @samples %>
</div>
