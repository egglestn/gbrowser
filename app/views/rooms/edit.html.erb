<% breadcrumb :room_edit, @room, @development, @development.developer %>

<h1><%= t("views.edit_name", name: @room.name) %></h1>

<%= render 'form', room: @room %>

<%= link_to 'Show', [@development, @room] %> |
<%= link_to 'Back', development_rooms_path(@development) %>
